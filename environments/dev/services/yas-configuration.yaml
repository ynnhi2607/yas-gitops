# Uses chart defaults from charts/yas-configuration.
# Keep environment-specific configuration here when needed.
mediaApplicationConfig:
  yas:
    publicUrl: http://api-dev.yas.local.com:31788/media
